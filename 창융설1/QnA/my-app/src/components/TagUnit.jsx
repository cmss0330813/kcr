import React from 'react';

function TagUnit( { tags }) {
    return (
            <div>
                {tags.map(tags => {
                    return (
                        {tags:name}
                    )
                })}
            </div>
    )
}

export default TagUnit;